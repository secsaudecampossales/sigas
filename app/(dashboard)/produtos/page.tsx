import { ModulePlaceholder } from "@/components/layout/module-placeholder";

export default function ProdutosPage() {
  return (
    <ModulePlaceholder
      title="Produtos"
      description="Cadastro único de materiais, categorias, unidades, estoque mínimo/máximo e controle por lote/validade quando aplicável."
    />
  );
}
