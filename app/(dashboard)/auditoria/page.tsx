import { ModulePlaceholder } from "@/components/layout/module-placeholder";

export default function AuditoriaPage() {
  return (
    <ModulePlaceholder
      title="Auditoria"
      description="Consulta de ações sensíveis registradas no sistema (usuário, entidade, data e contexto)."
    />
  );
}
